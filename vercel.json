{
  "outputDirectory": "prod",
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps --omit=optional --ignore-scripts",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

